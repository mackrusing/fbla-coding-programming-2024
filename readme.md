# 2024-coding-programming-bun

Create a new project.

```
bunx create-next-app --ts --tailwind --eslint --app --src-dir
```

Add prettier.

```
bun add -d prettier prettier-plugin-tailwindcss
```

Add & setup `shadcn/ui`.

```
bunx shadcn-ui@latest init
```
